/**
 * @deprecated Use `@/lib/api-client` instead.
 * This re-export exists to avoid breaking residual imports.
 * Remove after confirming no module imports from this path.
 */
export { api, ApiError, setGlobalToken } from './api-client'
